# geo-py

```
import geo

geo.read_tif('test.tif')

```

```
>>> import geo
>>> geo.
geo.add_watermark(  geo.gpd             geo.ogr             geo.shp_to_tif(
geo.avg_tif(        geo.if_overlap(     geo.os              geo.shutil
geo.cropToCutline(  geo.interpolate(    geo.osr             geo.SRS_transform(
geo.cut_tif(        geo.IPython         geo.plt             geo.tif_division(
geo.cv2             geo.json            geo.Polygon(        geo.tif_merge(
geo.Dataset(        geo.MFDataset(      geo.random          geo.tif_Resample(
geo.gdal            geo.mpl             geo.rasterizeLayer( geo.tif_to_shp(
geo.geo_py          geo.NC_to_tifs(     geo.read_tif(       geo.tqdm(
geo.glob(           geo.np              geo.shapefile       geo.write_tif(
```
